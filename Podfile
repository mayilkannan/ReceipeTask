# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Receipes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Receipes
	pod 'Firebase/Core'
	pod 'Firebase/Firestore'
  pod 'SDWebImage'
  target 'ReceipesTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
